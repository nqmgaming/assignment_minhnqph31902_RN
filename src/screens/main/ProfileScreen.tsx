import React from 'react';

import {Text, View} from 'react-native';

const ProfileScreen = () => {
    return (
        <Text>
            Profile
        </Text>
    );
};

export default ProfileScreen;
